% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/der_correct.R
\name{der_correct}
\alias{der_correct}
\title{Apply Selective Cholesky Correction}
\usage{
der_correct(x, method = "cholesky")
}
\arguments{
\item{x}{A \code{svyder} object with classification (from
\code{\link[=der_classify]{der_classify()}}).}

\item{method}{Correction method (default \code{"cholesky"}). Currently only
\code{"cholesky"} is supported.}
}
\value{
A \code{svyder} object with \code{corrected_draws},
\code{scale_factors}, and \code{original_draws} populated.
}
\description{
For each parameter flagged by \code{\link[=der_classify]{der_classify()}}, rescales the
posterior draws so that marginal variance matches the sandwich variance
estimate. The correction preserves the posterior mean: draws are centered,
scaled by \code{sqrt(V_sand[i,i] / sigma_mcmc[i,i])}, then re-centered.
}
\details{
Unflagged parameters retain their original draws without any modification.
}
\examples{
data(nsece_demo)
result <- der_compute(
  nsece_demo$draws,
  y = nsece_demo$y, X = nsece_demo$X,
  group = nsece_demo$group, weights = nsece_demo$weights,
  psu = nsece_demo$psu, family = "binomial",
  sigma_theta = nsece_demo$sigma_theta,
  param_types = nsece_demo$param_types
)
result <- der_classify(result, tau = 1.2, verbose = FALSE)
result <- der_correct(result)

}
\seealso{
\code{\link[=der_classify]{der_classify()}} for flagging parameters, \code{\link[=der_compute]{der_compute()}} for
computing DER, \code{\link[=as.matrix.svyder]{as.matrix.svyder()}} for extracting corrected draws.

Other core-pipeline: 
\code{\link{der_classify}()},
\code{\link{der_compute}()},
\code{\link{der_diagnose}()}
}
\concept{core-pipeline}
