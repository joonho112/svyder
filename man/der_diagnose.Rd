% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/der_diagnose.R
\name{der_diagnose}
\alias{der_diagnose}
\title{All-in-One DER Diagnostic}
\usage{
der_diagnose(x, ..., tau = 1.2, correct = TRUE)
}
\arguments{
\item{x}{A draws matrix (S x d), brmsfit, CmdStanMCMC, or stanreg object.
For the matrix method, columns 1:p are fixed effects and columns
(p+1):(p+J) are random effects.}

\item{...}{Additional arguments passed to methods.}

\item{tau}{Classification threshold (default 1.2).}

\item{correct}{Apply correction to flagged parameters (default \code{TRUE}).}
}
\value{
A fully processed \code{svyder} object with DER values,
classification, and (optionally) corrected draws.
}
\description{
Runs the full DER pipeline in a single call: \code{\link[=der_compute]{der_compute()}} to obtain
DER values, \code{\link[=der_classify]{der_classify()}} to assign tiers and flag parameters, and
optionally \code{\link[=der_correct]{der_correct()}} to apply selective correction. This is the
recommended entry point for most users.
}
\examples{
data(nsece_demo)
result <- der_diagnose(
  nsece_demo$draws,
  y = nsece_demo$y, X = nsece_demo$X,
  group = nsece_demo$group, weights = nsece_demo$weights,
  psu = nsece_demo$psu, family = "binomial",
  sigma_theta = nsece_demo$sigma_theta,
  param_types = nsece_demo$param_types
)
print(result)
summary(result)

}
\seealso{
\code{\link[=der_compute]{der_compute()}}, \code{\link[=der_classify]{der_classify()}}, \code{\link[=der_correct]{der_correct()}} for the
individual pipeline steps. \code{\link[=tidy.svyder]{tidy.svyder()}} and \code{\link[=glance.svyder]{glance.svyder()}} for
tidy summaries.

Other core-pipeline: 
\code{\link{der_classify}()},
\code{\link{der_compute}()},
\code{\link{der_correct}()}
}
\concept{core-pipeline}
