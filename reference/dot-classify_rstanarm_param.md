# Classify rstanarm parameter names

Classify rstanarm parameter names

## Usage

``` r
.classify_rstanarm_param(names)
```

## Arguments

- names:

  Character vector of rstanarm parameter names.

## Value

Character vector of types: "fixed", "random", or "other".
